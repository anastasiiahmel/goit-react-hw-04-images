import styled from 'styled-components';

export const ImageGalleryStyle = styled.ul`
  .container-gallery {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;

    padding: 0;
    margin: 0;
  }
`;
